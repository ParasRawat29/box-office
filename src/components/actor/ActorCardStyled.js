import styled from 'styled-components';
import { SearchCard } from '../Styled';

export const ActorCardStyled = styled(SearchCard)`
  .deathday {
    margin: 0;
    margin-top: 15px;
    font-weight: bold;
  }
`;
